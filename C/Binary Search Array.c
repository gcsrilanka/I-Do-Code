#include <stdio.h>
#include <stdlib.h>

int main()
{
    int arr[x],key,i;
    printf("enter array size");
    scanf("%d",&x);
    printf("enter values for array");
    for(x=0;x==x;x++)
    {
        scanf("value %d", &arr[x]);
    }
printf("enter key:");
scanf("%d",&key);

    for (i=0;i==x;i++)
    {
        if(key==arr[x])

        printf("position is %d", x+1);

    }
return 0;
}
